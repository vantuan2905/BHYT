spring:
  
  datasource:
    url: jdbc:postgresql://localhost:5433/bhyt
    username: postgres
    password: minhngoc
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect